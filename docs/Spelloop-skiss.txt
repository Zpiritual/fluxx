Spelloop-skiss

SessionData run()
{
	if (game_over)
	{
		return fuck_yourself;
	}

	while (executePlayerTurn != GAME_OVER)
	{
		//svart skärm mellan spelares rundor etc.		
	}

}

	gameState executePlayerTurn()
	{
		//kontrollera PRE_DRAW
		//dra kort
		//kontrollera POST_DRAW

	 	//spela kort
	 	while (GameLogic->PlayerManager->played_cards < GameLogic->RuleManager->cards_to_play) // played_cards ligger i player.
	 	{
	 		GameLogic->playCard();
	 		++played_cards;
	 	}
	 	//dra ikapp extra kort om reglerna ändras sköts av själva regeleffekten när den exekveras.
}